// app/dashboard/loading.tsx

// Al devolver null, le decimos a Next.js que no muestre ningún
// indicador de carga durante la navegación a esta página.
// La transición será instantánea una vez que el contenido esté listo.
export default function Loading() {
  return null;
}
